# Legacy course app (Spring Boot + Oracle 11g)

This folder contains the original CPS510 course submission: a Spring Boot MVC app (Thymeleaf) using Spring JDBC against an Oracle 11g schema.

It is **not** the primary portfolio build in this repository.

- **User guide**: `a9-user-guide.md`
- **Schema scripts**: `sql/`
- **Run**:

```bash
mvn spring-boot:run
```

