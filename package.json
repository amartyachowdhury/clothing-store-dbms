{
  "name": "clothing-store-dbms",
  "private": true,
  "description": "Clothing store admin monorepo (web + backend)",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "verify:lockfiles": "bash scripts/verify-lockfiles.sh",
    "sync:lockfiles": "bash scripts/sync-lockfiles.sh",
    "ci:web": "npm ci --prefix web && npm run lint --prefix web && npm run typecheck --prefix web && npm run build --prefix web",
    "ci:backend": "npm ci --prefix backend && npm run validate --prefix backend && npm run lint --prefix backend && npm run typecheck --prefix backend && npm run test --prefix backend && npm run build --prefix backend"
  }
}
