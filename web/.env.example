# Backend API (NestJS). Used by server components and server actions.
API_URL="http://localhost:4000"
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Server-only: must match backend API_KEY when auth is enabled (never expose in NEXT_PUBLIC_*)
API_KEY=
