# Backend API (NestJS). Used by server components and server actions.
API_URL="http://localhost:4000"
NEXT_PUBLIC_API_URL="http://localhost:4000"

# PostgreSQL (Prisma generate/migrate only; runtime data goes through the API)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/clothing_store?schema=public"
